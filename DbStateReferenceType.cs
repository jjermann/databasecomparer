using System;

namespace DatabaseComparer
{
    public enum DbStateReferenceType
    {
        Invalid,
        DbState,
        DbStateFilename,
        DbBackupFilename,
        DbName,
    }
}
